# ❌ VULNERABLE: allowPrivilegeEscalation not disabled
apiVersion: v1
kind: Pod
metadata:
  name: vulnerable-pod
spec:
  containers:
  - name: app
    image: nginx:1.24
    securityContext:
      # ⚠️ TRIGGER: allowPrivilegeEscalation defaults to true if not set

---
# ✅ SECURE: Privilege escalation explicitly blocked
apiVersion: v1
kind: Pod
metadata:
  name: secure-pod
spec:
  containers:
  - name: app
    image: nginx:1.24
    securityContext:
      allowPrivilegeEscalation: false  # ✅ OK: explicitly disabled